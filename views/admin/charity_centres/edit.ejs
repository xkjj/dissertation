<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Edit Charity Centre</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>

<body>

<div class="container mt-2 py-4 mb-4 bg-dark rounded-3 text-white">
    <div class="p-4">
        <h2 class="fw-bolder display-5">Edit Charity Centre</h2>
        <p class="lead">Update charity centre details</p>
    </div>
</div>

<div class="container">

    <form action="/admin/charity-centres/<%= centre.charity_id %>/edit" method="POST">

        <div class="mb-3">
            <label class="form-label">Charity Name</label>
            <input type="text" name="charity_name" class="form-control"
                   value="<%= centre.charity_name %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Address</label>
            <input type="text" name="charity_address" class="form-control"
                   value="<%= centre.charity_address %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Postcode</label>
            <input type="text" name="charity_postcode" class="form-control"
                   value="<%= centre.charity_postcode %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Email</label>
            <input type="text" name="charity_email" class="form-control"
                   value="<%= centre.charity_email %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Phone</label>
            <input type="text" name="charity_phone" class="form-control"
                   value="<%= centre.charity_phone %>">
        </div>

        <div class="mb-3">
            <label class="form-label">Status</label>
            <select name="is_active" class="form-select">
                <option value="1" <%= centre.is_active ? 'selected' : '' %>>
                    Active
                </option>
                <option value="0" <%= !centre.is_active ? 'selected' : '' %>>
                    Disabled
                </option>
            </select>
        </div>

        <button class="btn btn-dark">Save Changes</button>
        <a href="/admin/charity-centres" class="btn btn-outline-dark ms-2">
            Cancel
        </a>

    </form>

</div>

</body>
</html>
